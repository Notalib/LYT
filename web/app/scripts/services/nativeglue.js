'use strict';

/**
 * @ngdoc service
 * @name lyt3App.Nativeglue
 * @description
 * # Nativeglue
 * Service in the lyt3App.
 */
angular.module('lyt3App')
  .service('Nativeglue', function Nativeglue() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
